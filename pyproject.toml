[project]
name = "my-transcriber"
version = "0.1.0"
description = "Transcribe Finnish spoken notes from OGG audio files using faster-whisper"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=1.1.0",
]

[project.scripts]
transcribe = "transcribe:main"
